.sign_up__page
  .sign_up__page__header
    = image_tag ('material/logo/logo.png'), class: "logo"
  .sign_up__page__box
    .sign_up__page__box__title
      会員情報入力
    .sign_up__page__box__in  
      = form_for(@user, url: user_registration_path) do |f|
        = render "devise/shared/error_messages", resource: @user
        .field
          ニックネーム 
          %span 必須
          = f.text_field :nickname, autofocus: true, maxlength: "8", placeholder: "例) メルカリ太郎"
        .field
          メールアドレス
          %span 必須
          = f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "PC・携帯どちらでも可"
        .field
          = f.label "パスワード" 
          %span 必須     
          = f.password_field :password, autocomplete: "new-password", placeholder: "7文字以上の半角英数字"
        .field
          確認用パスワード
          %span 必須
          = f.password_field :password_confirmation, autocomplete: "new-password"
        %p
        本人確認
        安心・安全にご利用いただくために、お客さまの本人情報の登録にご協力ください。他のお客さまに公開されることはありません。  

        .field_set
          .field_name
            名字
            %span 必須
            = f.text_field :firstname, placeholder: "例) 山田"

          .field_name
            名前
           
            = f.text_field :lastname, placeholder: "例) 彩"

        .field_set
          .field_name
            名字(ふりがな)
            %span 必須
            = f.text_field :firstname_kana, placeholder: "例) やまだ"

          .field_name
            名前(ふりがな)
          
            = f.text_field :lastname_kana, placeholder: "例) あや"  

        
          
        .field
          生年月日
          %span必須
          -# = f.text_field :birth_day, placeholder: "例) 2000"
          = f.date_select :birthday, use_month_numbers: true,start_year: 1930, end_year: (Time.now.year - 10), default: Date.new(1989, 1, 1)


          
        .actions
        = f.submit "次へ進む"

     