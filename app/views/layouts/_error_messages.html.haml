= form_with(model: @product, local: true) do |form|
  - if @product.errors.any?
    #error_explanation
      %h2{:style => "color: red;"}
        = pluralize(@product.errors.count, "箇所")
        記入・選択漏れがあります: